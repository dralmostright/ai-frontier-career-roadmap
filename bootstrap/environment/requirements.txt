# Base toolchain for the AI Frontier Career Roadmap.
#
# Install with:   make setup
#
# Philosophy: pin minor versions, allow patches. You want reproducibility
# without fighting security patches for 18 months. Re-pin at each phase
# boundary (Months 4, 7, 10, 13, 16) and note the change in your monthly review.
#
# Heavy / optional dependencies are grouped at the bottom and commented out
# until the month that needs them. Installing PyTorch in Month 1 is 2GB of
# disk you don't need yet.

# ---------------------------------------------------------------------------
# Core scientific stack — Months 1+
# ---------------------------------------------------------------------------
numpy~=2.1
scipy~=1.14
pandas~=2.2
matplotlib~=3.9
seaborn~=0.13

# ---------------------------------------------------------------------------
# Classical ML — Months 2+
# ---------------------------------------------------------------------------
scikit-learn~=1.5
statsmodels~=0.14

# ---------------------------------------------------------------------------
# Notebooks — Months 1+
# ---------------------------------------------------------------------------
jupyterlab~=4.2
ipykernel~=6.29
ipywidgets~=8.1

# ---------------------------------------------------------------------------
# Testing and quality — Months 1+ (non-negotiable, install on day one)
# ---------------------------------------------------------------------------
pytest~=8.3
pytest-cov~=5.0
hypothesis~=6.112
ruff~=0.6
mypy~=1.11

# ---------------------------------------------------------------------------
# Config and CLI — Months 5+
# ---------------------------------------------------------------------------
pydantic~=2.9
pydantic-settings~=2.5
pyyaml~=6.0
typer~=0.12
rich~=13.8
python-dotenv~=1.0

# ---------------------------------------------------------------------------
# Gradient boosting — Month 3
# ---------------------------------------------------------------------------
# xgboost~=2.1
# lightgbm~=4.5

# ---------------------------------------------------------------------------
# Deep learning — Months 4+
# Install the build that matches your hardware. On Apple Silicon the default
# wheel gives you MPS. On Linux+NVIDIA use the CUDA index URL from pytorch.org.
# ---------------------------------------------------------------------------
# torch~=2.4
# torchvision~=0.19
# torchmetrics~=1.4
# tensorboard~=2.17

# ---------------------------------------------------------------------------
# NLP and LLMs — Months 7+
# ---------------------------------------------------------------------------
# transformers~=4.44
# tokenizers~=0.19
# datasets~=3.0
# sentence-transformers~=3.1
# accelerate~=0.34

# ---------------------------------------------------------------------------
# Fine-tuning — Month 12
# ---------------------------------------------------------------------------
# peft~=0.13
# trl~=0.11
# bitsandbytes~=0.44

# ---------------------------------------------------------------------------
# RAG and serving — Months 10+
# ---------------------------------------------------------------------------
# fastapi~=0.115
# uvicorn~=0.30
# httpx~=0.27
# psycopg[binary]~=3.2
# pgvector~=0.3
# sqlalchemy~=2.0
# alembic~=1.13
# rank-bm25~=0.2
# faiss-cpu~=1.8

# ---------------------------------------------------------------------------
# LLM providers — Months 10+ (needs API keys in .env)
# ---------------------------------------------------------------------------
# anthropic~=0.34
# openai~=1.47

# ---------------------------------------------------------------------------
# MLOps and distributed — Months 13+
# ---------------------------------------------------------------------------
# mlflow~=2.16
# ray[data]~=2.35
# prometheus-client~=0.21
# evidently~=0.4
